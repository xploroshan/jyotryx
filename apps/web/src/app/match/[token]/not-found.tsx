import SharedMatchNotFound from "@/components/match/SharedMatchNotFound";

/** Rendered when the shared-match page calls `notFound()` for an unknown token. */
export default function NotFound() {
  return <SharedMatchNotFound />;
}
