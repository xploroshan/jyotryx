apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: myastro360
  labels:
    app: api
    app.kubernetes.io/part-of: myastro360
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
