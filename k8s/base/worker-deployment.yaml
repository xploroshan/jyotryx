apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: myastro360
  labels:
    app: worker
    app.kubernetes.io/part-of: myastro360
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
        app.kubernetes.io/part-of: myastro360
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: worker
          image: ghcr.io/xploroshan/myastro360-api:latest
          command: ["node", "dist/main", "--worker"]
          envFrom:
            - configMapRef:
                name: myastro360-config
            - secretRef:
                name: myastro360-secrets
          env:
            - name: WORKER_MODE
              value: "true"
          readinessProbe:
            exec:
              command: ["sh", "-c", "node -e 'process.exit(0)'"]
            initialDelaySeconds: 10
            periodSeconds: 30
          livenessProbe:
            exec:
              command: ["sh", "-c", "node -e 'process.exit(0)'"]
            initialDelaySeconds: 15
            periodSeconds: 30
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 15"]
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1Gi
