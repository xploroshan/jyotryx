apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: myastro360
  labels:
    app: web
    app.kubernetes.io/part-of: myastro360
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
